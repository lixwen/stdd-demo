schema: stdd

context: |
  Tech stack: Python 3, FastAPI, Pydantic, pytest, httpx
  Project: A simple REST API for validating the STDD workflow
  Conventions: Use virtual environment at .venv, follow PEP 8
  Testing: pytest with httpx async client for API tests
